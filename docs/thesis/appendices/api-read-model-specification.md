# Додаток А. API read-model специфікація

Цей додаток узагальнює FastAPI-методи, які використовуються в дипломному
проєкті для подання стану pipeline, операторських preview та дослідницьких
результатів. API-шар не є стратегічним рушієм і не виконує ринкові заявки. Його
роль полягає в тому, щоб надати стабільні read-model контракти поверх Dagster
assets, Postgres/read stores, forecast store, telemetry store та локальних
configuration sources.

Повна технічна документація endpoint-ів підтримується окремо в
`docs/technical/API_ENDPOINTS.md`. У цьому додатку наведено thesis-facing
специфікацію: які дані стоять за кожним методом, яку частину методології він
підтримує та яку межу твердження має.

| Метод і шлях                                        | Параметри                                                             | Джерело даних                                                                                  | Короткий зміст відповіді                                                                      | Пов'язаний Dagster asset/store                                        | Роль                                                           | Межа твердження                                                            |
| --------------------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `GET /health`                                       | Немає                                                                 | Стан API-процесу                                                                               | Мінімальний статус доступності                                                                | FastAPI runtime                                                       | Технічна перевірка доступності API                             | Не є evidence або strategy method.                                         |
| `GET /tenants`                                      | Немає                                                                 | Tenant registry, location metadata                                                             | Tenant IDs, names, coordinates, timezone                                                      | Tenant configuration registry                                         | Вибір tenant context для experiments/read models               | Конфігураційний read model.                                                |
| `POST /weather/run-config`                          | `tenant_id`, optional `location_config_path`                          | Tenant registry, weather location resolver                                                     | Dagster run config і resolved location                                                        | `weather_forecast_bronze` config path                                 | Підготовка location-aware weather materialization              | Не матеріалізує strategy evidence.                                         |
| `POST /weather/materialize`                         | `tenant_id`, `include_price_history`, optional `location_config_path` | Tenant registry, Open-Meteo weather config, optional DAM price history                         | Selected assets, run config, materialization success                                          | `weather_forecast_bronze`, optional `dam_price_history`               | Bronze ingestion/demo materialization control                  | Demo/materialization control; не є доказом ML-переваги.                    |
| `GET /dashboard/signal-preview`                     | `tenant_id`, optional `location_config_path`                          | Weather rows, tenant-aware price context, operator status persistence                          | Market/weather signal preview and resolved location                                           | Weather/market preview helpers, operator status store                 | Operator-facing explanatory preview                            | Read model only; не створює dispatch або bid.                              |
| `GET /dashboard/operator-status`                    | `tenant_id`, `flow_type`                                              | Persisted operator status records                                                              | Latest visible state for a selected operator flow                                             | Operator status store                                                 | Reproducible state reporting for demo/control flows            | Лише status contract.                                                      |
| `POST /dashboard/projected-battery-state`           | Tenant, battery metrics, starting SOC, schedule                       | Request payload, tenant defaults, projected battery simulator                                  | Hourly SOC trace, throughput, degradation-aware economics                                     | Projected battery simulator                                           | Feasibility-and-economics preview for a proposed schedule      | Simulation/read model; не є фізичним dispatch.                             |
| `GET /dashboard/battery-state`                      | `tenant_id`                                                           | MQTT telemetry ingest path, Postgres telemetry rows, hourly snapshots                          | Latest telemetry, hourly snapshot, freshness and fallback status                              | Battery telemetry store                                               | SOC-aware operator context                                     | Physical-state read model; не є control command.                           |
| `GET /dashboard/exogenous-signals`                  | `tenant_id`                                                           | Weather metadata, public grid-event read model                                                 | Latest weather and grid-event explanatory signals                                             | Weather/grid-event assets and helpers                                 | Exogenous context for explanation and future forecast features | Explanatory covariates; не є direct LP control input.                      |
| `GET /dashboard/baseline-lp-preview`                | `tenant_id`                                                           | Tenant defaults, strict similar-day price history, LP solver                                   | Forecast, signed MW schedule, projected SOC, UAH economics                                    | Baseline LP solver, projected battery simulator                       | Level 1 MVP baseline preview                                   | Recommendation preview; no `ProposedBid`, clearing, or dispatch semantics. |
| `GET /dashboard/forecast-strategy-comparison`       | `tenant_id`                                                           | Persisted strategy evaluation rows                                                             | Comparison of strict similar-day, NBEATSx and TFT after LP/oracle scoring                     | Strategy evaluation store                                             | Forecast-to-schedule decision-value evidence                   | Read model рівня Gold; не є ринковим виконанням.                           |
| `GET /dashboard/real-data-benchmark`                | `tenant_id`                                                           | Persisted observed-data rolling-origin benchmark rows                                          | Benchmark summary, rows, data-quality tier and regret metrics                                 | Strategy evaluation store                                             | Thesis-grade benchmark evidence                                | Лише observed-data evaluation.                                             |
| `GET /dashboard/calibrated-ensemble-benchmark`      | `tenant_id`                                                           | Persisted calibrated value-aware ensemble rows                                                 | Selected model, selection policy and prior validation context                                 | Strategy evaluation store                                             | Calibration/selector diagnostic evidence                       | Research read model; не є dashboard default promotion.                     |
| `GET /dashboard/risk-adjusted-value-gate`           | `tenant_id`                                                           | Persisted risk-adjusted gate rows                                                              | Median/tail/win-rate selector diagnostics                                                     | Strategy evaluation store                                             | Risk-aware selector diagnostics                                | Діагностичний метод; не змінює стратегію без проходження promotion gate.    |
| `GET /dashboard/forecast-dispatch-sensitivity`      | `tenant_id`                                                           | Horizon-aware regret-weighted benchmark payloads                                               | Regret buckets, forecast error and LP sensitivity diagnostics                                 | Strategy evaluation store                                             | Пояснення причин втрати value у forecast-derived schedule      | Explainability read model; не є виконанням стратегії.                      |
| `GET /dashboard/future-stack-preview`               | `tenant_id`                                                           | Forecast store, strategy evaluation store, backend/runtime status                              | NBEATSx/TFT forecast series, quality boundary, runtime availability                           | Forecast store, strategy evaluation store                             | Future forecast-stack preview and defense visualization        | Forecast evidence surface; не є promoted dispatch policy.                  |
| `GET /dashboard/dfl-relaxed-pilot`                  | `tenant_id`                                                           | Persisted relaxed-LP DFL pilot rows                                                            | Relaxed DFL pilot diagnostics for selected tenant                                             | DFL training store                                                    | Differentiable optimization research primitive                 | Не є повним DFL-твердженням.                                               |
| `GET /dashboard/dfl-schedule-value-production-gate` | Немає                                                                 | Persisted Schedule/Value Learner promotion rows                                                | Source-specific gate rows, promotion flags, regret metrics                                    | DFL training store, `dfl_schedule_value_production_gate_rows`         | V2/V2+ Offline Strategy Promotion evidence                     | Offline/read-model evidence; `market_execution_enabled=false`.             |
| `GET /dashboard/decision-transformer-trajectories`  | `tenant_id`, optional `limit`                                         | Persisted offline trajectory rows                                                              | DT training/evaluation trajectory rows                                                        | Simulated trade store, `decision_transformer_trajectories`            | Offline DT dataset inspection                                  | Лише training/evaluation data; не є live policy actions.                   |
| `GET /dashboard/decision-policy-preview`            | `tenant_id`, optional `limit`                                         | Persisted offline DT policy-preview rows                                                       | Projected actions, safety projection, value gap and policy context                            | Simulated trade store, `decision_transformer_policy_steps`            | DT policy-preview diagnostics                                  | Лише preview; не є ринковим виконанням.                                    |
| `GET /dashboard/simulated-live-trading`             | `tenant_id`, optional `limit`                                         | Persisted simulated replay rows                                                                | Simulated replay timeline and outcomes                                                        | Simulated trade store, `simulated_live_trading_rows`                  | Future execution-surface rehearsal                             | Лише simulated rows; немає реальних settlement identifiers.                |
| `GET /dashboard/operator-recommendation`            | `tenant_id`, optional `strategy_id`                                   | Battery state, tenant load/PV context, strategy availability, forecast/read stores, LP preview, V13 acquisition packet | DAM hourly scope metadata, anchor/forecast timing, selected strategy, V13 readiness with receipt-source audit freshness, forecast series, value-gap series, feasible schedule | Battery telemetry store, strategy/forecast stores, LP preview helpers, V13 packet export | Продуктовий read model для операторського перегляду             | DAM-only recommendation preview; `market_execution_enabled=false`; no `ProposedBid`, no IDM bid recommendation, no market submission; DT/LAVA strategy selection remains blocked while `v13_readiness.dt_lava_ready=false`. |
| `GET /dashboard/shadow-recommendation-preview`      | `tenant_id`, `preview_source`                                         | DT shadow selected-schedule packet, V2+/TFT/DFL candidate artifacts, V13 readiness blockers    | Manual shadow preview source, expanded hourly schedule rows, candidate/schedule family, regret/value versus V2+ and strict reference, non-promotion labels | DT research-shadow packet, candidate schedule artifacts               | Defense/operator diagnostic strategy switch                    | Manual preview only; default remains V2+; `market_execution_enabled=false`; no `ProposedBid`; no market order payload; blocked V13/DT/LAVA training remains roadmap evidence. |

Ці endpoint-и слід трактувати як групи read-model
контрактів. Детальні параметри та response fields важливі для інженерної
відтворюваності, але методологічний висновок залишається незмінним: якість
ML/DFL-кандидатів визначається rolling-origin LP/oracle evaluation та promotion
gates, а не самим фактом наявності API endpoint-а.

Операторський dashboard тому має два різні режими читання. За замовчуванням
використовується `GET /dashboard/operator-recommendation`, де V2+ залишається
teacher/comparator/fallback і default honest strategy. Manual switch у UI може
завантажувати `GET /dashboard/shadow-recommendation-preview`, щоб показати DT
Shadow, Poland/TFT Shadow або DFL diagnostics як власні hourly recommendation
tables. Цей другий режим не змінює production/default selection і не є
ринковою заявкою.

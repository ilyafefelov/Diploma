> *This was generated by AI during triage.*

# Enhancement: Розширити control-plane API до повного MVP slice

## Problem

Поточний FastAPI surface в [api/main.py](d:/School/GoIT/Courses/Diploma/api/main.py) закриває лише tenant selection, weather run-config preview та weather/price-history materialization. Для dashboard наступного етапу цього недостатньо: UI ще не може стабільно отримати Baseline Forecast, Baseline Strategy output, gatekeeper status та зручний status model для останнього MVP run.

## Why Now

- Поточний API вже валідований як control-plane база.
- Наступний фронтенд етап буде штучно гальмуватися без ширшого read/query surface.
- Якщо не додати узгоджені MVP endpoints зараз, dashboard почне напряму залежати від внутрішніх Dagster/asset details.

## Scope

- Додати tenant-aware endpoint для повного MVP demo materialization або orchestration.
- Додати read endpoints для останніх результатів MVP slice:
  - enriched DAM history
  - strict similar-day forecast
  - baseline dispatch plan summary
  - validated dispatch outcome
  - regret / oracle comparison summary
- Узгодити response contracts через Pydantic v2.
- Описати boundary між control-plane і read-model API.

## Acceptance Criteria

- Є хоча б один endpoint, який materialize-ить або повертає весь MVP slice, а не лише weather assets.
- Є окремі read endpoints або один consolidated summary endpoint для dashboard consumption.
- Усі нові endpoints мають OpenAPI summaries/descriptions і markdown docs.
- Є інтеграційні FastAPI тести на happy path і базові failure paths.
- Dashboard не повинен знати Dagster internals, щоб отримати baseline/regret/gatekeeper summary.

## Suggested Labels

- enhancement
- ready-for-agent
- api
- mvp

## Notes For Implementation

- Спиратися на термінологію з [CONTEXT.md](d:/School/GoIT/Courses/Diploma/CONTEXT.md).
- Не протікати asset internals у public contract сильніше, ніж потрібно для operator UI.
- Залишити Proposed Bid / Cleared Trade API поза scope, якщо це не потрібно для найближчого dashboard slice.
> *This was generated by AI during planning.*

# Enhancement: Persist latest operator-visible status by tenant and flow

## Parent

Derived from [docs/technical/PRD-operator-mvp-slices.md](d:/School/GoIT/Courses/Diploma/docs/technical/PRD-operator-mvp-slices.md).

## What to build

Create the first lightweight Postgres-backed operator status store that persists the latest operator-visible result for a `(tenant_id, flow_type)` pair. This slice should cover Slice 1 flows first and establish the stable contract for backend-owned status semantics.

## Acceptance criteria

- [ ] A lightweight Postgres table exists for latest operator-visible state keyed by `tenant_id + flow_type`.
- [ ] The canonical status values `idle`, `prepared`, `running`, `completed`, `failed` are represented by backend contracts, not inferred by frontend code.
- [ ] The implementation persists and retrieves latest visible state for `weather_control` and `signal_preview`.
- [ ] The slice is demoable through a backend/API path without relying on in-memory process state.

## Blocked by

None - can start immediately.

## Notes

- Keep scope narrow: latest visible result only, not full event log.
- Reserve future `flow_type` values without implementing their business slices yet.
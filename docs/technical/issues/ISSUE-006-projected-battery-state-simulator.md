> *This was generated by AI during planning.*

# Enhancement: Build projected battery state simulator for Level 1 LP preview

## Parent

Derived from [docs/technical/PRD-operator-mvp-slices.md](d:/School/GoIT/Courses/Diploma/docs/technical/PRD-operator-mvp-slices.md).

## What to build

Create a narrow `Projected Battery State` simulator for Slice 2 that runs over hourly DAM intervals and transforms a signed MW recommendation schedule into projected SOC, throughput, and degradation-aware economics. This slice is for LP preview only and must not expand into a full digital twin.

## Acceptance criteria

- [ ] The simulator accepts latest known asset state with allowed scenario override for offline/demo use.
- [ ] The simulator works at hourly granularity aligned with `Level 1 Market Scope`.
- [ ] The simulator enforces `soc_min`, `soc_max`, capacity, power limits, and charge/discharge efficiency.
- [ ] The simulator returns projected SOC trace, throughput, and degradation penalty in UAH.

## Blocked by

None - can start immediately.

## Notes

- Do not add temperature, SOH, SEI, or other digital-twin physics in this slice.
- This slice should stay testable as a deep, isolated module.
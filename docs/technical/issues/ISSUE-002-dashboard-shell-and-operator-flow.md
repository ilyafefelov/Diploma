> *This was generated by AI during triage.*

# Enhancement: Побудувати новий operator dashboard поверх tenant-aware API

## Problem

Наявний UI базується на legacy dashboard у [_legacy_smart-energy-ai/dashboard](d:/School/GoIT/Courses/Diploma/_legacy_smart-energy-ai/dashboard), але наступний інтерфейс має бути чистішим, акуратнішим і візуально сильнішим. Legacy layout не відображає нову control-plane модель і не підходить як є для tenant-aware MVP workflow.

## Why Now

- API foundation для tenant selection та materialization вже з'явилась.
- Далі користувач хоче перейти саме до нового dashboard, а не розвивати legacy view.
- Без окремого shell design фронтенд швидко перетвориться на набір несумісних сторінок і випадкових chart blocks.

## Scope

- Створити новий dashboard shell для operator workflow:
  - tenant selector
  - resolved location / config preview
  - materialization trigger
  - latest run status panel
  - data surfaces для DAM, weather, forecast, dispatch, regret
- Використати native fetch / $fetch замість axios у нових data flows.
- Відділити control actions від analytical read views.
- Закласти mobile-safe, але desktop-first layout.

## Acceptance Criteria

- Є чіткий app shell для operator scenario, а не marketing layout.
- Tenant selection, config preview і materialize action доступні з одного workflow.
- UI показує resolved location як частину operational truth.
- Новий код не залежить від axios.
- Підготовлена структура для підключення richer chart surfaces.

## Suggested Labels

- enhancement
- ready-for-agent
- dashboard
- ux

## Notes For Implementation

- Візуально уникати generic card soup.
- Орієнтуватися на щільний аналітичний workspace, а не на landing-page патерни.
- Реюз існуючого Nuxt runtime допустимий, але visual language треба суттєво оновити.
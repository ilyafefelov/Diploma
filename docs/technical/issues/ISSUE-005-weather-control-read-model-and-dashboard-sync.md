> *This was generated by AI during planning.*

# Enhancement: Wire weather control state through persisted read models into the dashboard

## Parent

Derived from [docs/technical/PRD-operator-mvp-slices.md](d:/School/GoIT/Courses/Diploma/docs/technical/PRD-operator-mvp-slices.md).

## What to build

Extend the current Tenant Registry -> weather run-config -> weather materialization -> operator dashboard path so that the dashboard reads the latest persisted weather-control state from backend-owned read models instead of reconstructing semantics in frontend composables.

## Acceptance criteria

- [ ] The dashboard receives weather-control status from a backend read model instead of inventing status transitions client-side.
- [ ] The dashboard shows latest run-config preparation and materialization outcome after process restart.
- [ ] The UI still supports the existing end-to-end operator flow for tenant selection, run-config preview, and materialization.
- [ ] API documentation for the Slice 1 read model is updated and demo-ready.

## Blocked by

- [docs/technical/issues/ISSUE-004-persisted-operator-status-read-model.md](d:/School/GoIT/Courses/Diploma/docs/technical/issues/ISSUE-004-persisted-operator-status-read-model.md)

## Notes

- Keep control actions and analytical surfaces separate.
- Treat this slice as the canonical completion point for Slice 1.